# Ubuntu 14.04
image: ubuntu:trusty

before_script:
  - apt-get update -qq && apt-get install -y -qq build-essential
  - pwd

build:
  stage: build
  script:
    - pwd
    - ls
    - mkdir build
    - cd build
    - cmake ..
    - make
